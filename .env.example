# ─────────────────────────────────────────────
# claude-code-agent-wa configuration
# Copy this file to .env and fill in your values
# ─────────────────────────────────────────────

# Required: Your WhatsApp number (without + or spaces)
# Multiple numbers: separate with commas
WHATSAPP_NUMBERS=5511999999999

# System prompt for the AI assistant
SYSTEM_PROMPT=You are a helpful AI assistant on WhatsApp. Be concise and direct.

# Claude model: sonnet (fast) or opus (smart)
CLAUDE_MODEL=sonnet

# Working directory for Claude Code (where your CLAUDE.md is)
# This gives Claude context about your project
PROJECT_DIR=.

# Optional: Override Claude CLI path (auto-detected by default)
# CLAUDE_CLI_PATH=/path/to/claude

# Optional: Claude timeout in ms (default: 300000 = 5 min)
# CLAUDE_TIMEOUT=300000

# Optional: Max conversation history messages per chat (default: 20)
# MAX_HISTORY=20

# Optional: Respond to your own messages from another device (default: true)
# SELF_CHAT_MODE=true

# ─── Voice (Optional) ───────────────────────
# ElevenLabs for voice responses (leave empty to disable)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# Voice reply mode: auto (reply audio to audio), always, never
VOICE_REPLY_MODE=auto

# ─── PDF Generation (Optional) ──────────────
# Chrome/Chromium path for HTML→PDF conversion
# macOS default: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
# Linux default: /usr/bin/google-chrome
# CHROME_PATH=/usr/bin/google-chrome
